```meta
suite: literature-review
version: 2.0
```

# 文献综述 Prompt Suite v2

> 统一管理所有综述相关 prompt。新增模板:在下方 ` ```section <id>` 创建,正文用 `{{base:<rule-id>}}` 引用共享规则。
> 调用方式:`PromptTemplate.load("literature-review:<id>")`

```section
### {{base:academic-tone}}

学术写作语气。所有生成类模板(screening 除外)必须遵守:

- 第三人称客观表述,严禁"我/我们/笔者/本文作者"
- 学术话语体系:考察、检视、辨析、回顾、梳理、剖析、管窥、审视、探究
- 避免空泛强度词前缀:"深入分析"、"系统梳理"、"全面探讨"、"详细阐述"、"极大地"、"显著地"
- 句长变异:目标方差 ≥8,避免每句 18-25 字的均匀长度
- 诚实性表述:"可能"、"或许"、"有待验证"、"尚需进一步"、"值得探究"——适度使用,是学者口吻

### {{base:citation-rules}}

引用规范。section 模板必须遵守:

**基本格式**:

- 正文只写从文献清单复制的"作者(年份)"夹注,例如:`张三(2024)认为...。`
- `[lit_xxx]` 锚点由后端根据"作者(年份)"与真实文献元数据的映射自动注入,模型严禁自行填写、移动或编造锚点。
- 同一 lit_id 在同文献综述内只允许出现一次
- comment 章节(文献述评)严禁任何引用,包括 `[lit_xxx]` 与"作者(年份)"
- 作者人数:中文 2 人用"和",3 人及以上用"等";英文 2 人用 "和",3 人及以上用 "et al."

**硬约束(代码层也会校验)**:

- **每提到一篇 paper,必须写出与文献清单一致的作者(年份)夹注**。后端只会为能命中本章真实文献池的夹注注入锚点;未命中的作者(年份)不会生成链接,也不会虚构文献。
- 锚点位置、合法性和同一文献去重由后端代码强制处理,模型不要输出任何 `[]` 引用标记。
- **严禁编造文献作者(年份)**:你只能写下方 `papers_catalog` 里出现过的论文作者与年份。如果某个观点在本章文献池里找不到支撑,**直接换观点或承认"现有研究较少"**,不要凭训练数据里的常识补一篇不存在的文献(医学/营养学/公共卫生等高频领域尤其容易踩坑)。

**主题相关性红线(代码层兜底 + prompt 双闸)**:

- 所有论述必须围绕本章 `papers_catalog` 里真实收录的文献展开。
- 不要在可用文献池以外引用其他研究方向;若文献池里的主题与主研究方向(如主题词A/主题词B/主题词C)不匹配,**承认"本章文献较少"**即可,不要捏造内容凑字。
- 不出现 `[lit_xxx]` 锚点的位置,任何"(作者, 年份)"夹注都必须能在本章文献池里找到对应;否则后端会标记为 `dropped_citations` 并告警,前端会拒绝渲染。

### {{base:anti-ai-academic}}

学术综述专属反 AI 痕迹。section/humanize 模板必须遵守:

**学术八股句式(必须改写)**:

- "X 是 Y 的重要组成部分/关键环节"
- "为 X 提供了新的视角/思路/方法"
- "对 X 进行了深入的探讨/分析"
- "具有重要的理论意义和实践价值"
- "极大地/显著地促进了 X"
- "对于 X 具有重要的启示意义"
- "在 X 方面取得了显著进展"
- "值得 X 借鉴/参考"

**话术堆叠限制**(每段最多 1 次):

- "研究表明"、"学者认为"、"有学者指出"、"相关文献显示"、"大量文献表明"、"众多学者"

**连接词限制**:

- 禁三段式:"首先...其次...最后" / "第一...第二...第三"
- 否定排比每段 ≤1 次:"不仅...而且..." / "既...又..." / "这不仅仅是...而是..."
- "然而"、"此外"、"因此"每段 ≤1 次

**段落结构**:

- 禁完美四段式(主题句+引用+分析+小结的固定模板)
- 段落结尾多样化,避免每段都"干净一行句号"
```

### }

写作硬规则。section 模板必须遵守:

**字段拷贝**:作者、年份、期刊名、卷期号**必须**从下方 `papers_catalog` 字段直接复制,

禁止凭印象编造(常见错例:把 2025 写成 2024、把期刊名简化成错误名)。

正文里每出现一次「作者(年份)」,年份与 catalog 中该 lit_id 的「年份」字段**逐字相同**。

**引用密度**:本章允许引用的文献池里至少 15 篇时,**正文中至少使用 8 个不同的 `[lit_xxx]` 锚点**,

且同一 lit_id 在本章只允许出现 1 次(去重由代码层强制)。禁止一章只引 2-3 篇。

少于 15 篇时按比例下限 0.5× 设置。

**反八股红线**(违反任意一条即视为不合格):

- "取得了显著进展" / "取得了丰硕成果" / "取得了明显效果" — 用具体指标替代
- "研究呈现明显的分歧" — 直接说"两类研究的差异在于 X 与 Y"
- "为 X 提供了新的视角/思路/方法" — 删掉,改为具体结论
- "对于 X 具有重要的启示意义" — 直接说"对 X 有两点借鉴"
- "极大地/显著地促进了 X" — 用具体数据替代
- 段落开头禁用 "近年来"、"随着 X 的发展"、"在 X 背景下" — 用具体时间锚点替代
- 段落结尾禁用空泛判断 "值得进一步研究" / "有待深入" — 改为具体 Gap

**正文密度**:每章 ≥ 800 中文字符;comment 章 ≥ 400 字(短小犀利,不重复正文)。

```



```section
你负责筛选主题相关的学术论文。

## 任务

基于论文的标题与摘要,判断每篇是否与研究主题实质相关。

## 实质相关判定

- 研究主题/对象/方法/理论/应用任一层面有交集 → 视为相关
- 跨学科无连接、研究对象差异大 → 视为无关
- 无摘要文献:默认剔除(无法判断相关性)

## 摘要处理

- 优先提供完整摘要;若超过 1000 字,截取前 1000 字

## 输入

研究主题: {{topic}}
文献清单(数组): {{papers_json}}

每个元素: `{"lit_id": str, "title": str, "abstract": str(可能为空)}`

## 输出

严格 JSON 数组,无任何额外文字:

```json
[
  {"lit_id": "<原文>", "relevant": true|false, "reason": "<一句话中文原因>"}
]
```

```

```section classify
你负责对文献池进行分组,为后续综述章节写作准备。

{{base:academic-tone}}

## 任务

按 {{classify_mode}} 方式分组。

当 `locale`:

- 国外组: source = "openalex" 的英文文献
- 国内组: source = "user_imported" 的中文文献
- **不要**根据期刊/作者国籍二次判断

当 `theme`:

- **硬约束:至少 3 组,文献池 ≥30 篇时至少 4 组**;严禁只输出 1 个总主题。
- **分组依据是每篇文献的摘要内容**(文献清单中每行末尾附有摘要节选):
  按摘要反映的研究问题、方法、数据与情境判断主题归属,标题只作辅助。
  严禁只按标题字面词或期刊来源凑组。
- **组名必须用中文**。综述正文语言是中文,即使某组全是英文文献,
  也要用中文概括其研究角度;**严禁**直接用英文单词当组名。
- **组名要像学术综述章节标题** — 4~10 字,简洁有信息量,反映本组的研究角度或方法维度;
  - **可**(但不必一定)在标题里体现主题的核心研究对象(即下方"研究主题"中的关键名词)。
  - 组名必须体现本组文献的具体研究角度,让读者不看正文也能知道这组讲什么。
- **拆分维度优先顺序**(按本主题文献的实际构成选择):
    1. **研究问题/方法差异**:不同建模口径、算法流派、理论视角
    2. **应用情境差异**:不同行业、场景、对象群体
    3. **机制/要素差异**:研究对象的不同构成要素或环节
  当文献标题高度相似(如同一主轴下的不同方法研究)时,必须读摘要按维度 1(方法)拆开。
- **不要**写检索方式、数据库来源、筛选流程。
- 各组文献量大致均衡(一般每组 6~25 篇);某组不足 5 篇则并入最相近的主题。
- **禁止**"其他"或"杂项"兜底分组(无法归入的文献由代码层处理)。

## 输入

研究主题: {{topic}}
分类方式: {{classify_mode}}
文献清单: {{papers_catalog}}

## 输出

严格 JSON:

```json
{
  "groups": [
    {"name": "<主题名>", "ids": [1, 2, 3]}
  ]
}
```

`ids` 只填文献清单每行行首的编号数字(`#` 后的整数),严禁抄写 lit_id 原文 ——
大文献池时回显 lit_id 会超出输出上限导致 JSON 被截断。

## 强约束

1. 每篇文献(编号)只能归入一个组
2. 不允许遗漏

```

```section section
你负责撰写硕士论文文献综述的单节正文。

{{base:academic-tone}}
{{base:citation-rules}}
{{base:anti-ai-academic}}

## 本节身份

- 主题: {{topic}}
- 章节: {{section_title}} (key={{section_key}})
- 本节定位: {{section_role}}

## 文献池

{{papers_catalog}}

写作约束:

- **有摘要的文献**: 必须基于摘要内容改写,不得直接复述
- **无摘要的文献**: 仅基于标题/作者/年份/期刊概括研究方向,**禁止**编造数据或结论

## 可用 lit_id 列表(严格从这里选)

{{available_lit_ids}}

## 字数与结构

- 本节正文目标 **1200~2000 字**(综述全文 6-10K 字,本节占 1/章节)
- 段落 5~8 段为宜
- **不强制**完美四段式——允许段落长短交错

## 引用密度(代码层会硬校验)

- 本章文献池 ≥ 5 篇时,**正文中至少出现 5 处不同的「作者(年份)」夹注**(分别对应不同 lit_id);少于 5 篇时按比例,但**至少 2 处**。
- 即使文献池里只有 1 篇文献,本节也必须至少引用它一次。
- 同一 lit_id 在本节只允许出现一次。
- 引用不是为了堆叠,而是基于该文献的具体观点做支撑;若文献池文献少,在「作者(年份)」后跟一句话总结该文献的核心论点即可。

## 主题连贯性

- 章末段落应呼应前章或预告下章(让综述读起来像一个连贯叙事,而不是孤立章节)
- 过渡段可以弱引或不引

## 覆盖不足的诚实声明

- 当某主题文献稀少时,直接表述"现有研究较少"、"文献较为有限"、"尚需进一步探讨"
- **禁止**编造"研究表明"补缺
- **禁止**在文献池与主研究方向不一致时,凭常识拼凑其他领域(医学/公共卫生/教育学 等)的"作者(年份)"夹注 — 后端会全部丢弃并告警,前端会显示「检测到幻觉引用已剥离」

## 信息缺失文献处理

- 无作者:用期刊名代指(如"据《中国渔业经济》报道"),**严禁使用"佚名"**
- 无摘要:仅基于标题概述,禁止虚构数据
- 无年份:省略"作者(年份)"夹注,改用纯文字表述

## 输出

- 仅输出章节正文,不要 markdown 标题、序号、开场白
- 正文中所有 `[lit_xxx]` 必须严格属于可用 lit_id 列表
- 同一 lit_id 在本节只允许出现一次
- 若写了不属于列表的 lit_id,该段会被废弃

{{role_hint}}
```

```section
你负责将学术综述中的 AI 写作痕迹去除,使文本读起来更接近真人学者书写。

{{base:academic-tone}}
{{base:anti-ai-academic}}

## 硬约束(不可违反)

1. **必须严格按下方 `## 输出格式` 的 JSON 结构输出**,不要任何解释、思考过程、Markdown 标题或开场白
2. **不要输出 "以下是改写" / "I will..." / "Let me think..." 等任何英文/中文前后缀**
3. **rewritten 字段必须是可直接复用的最终文本**(不是 "改写后的段落:xxx" 这种带标签的)
4. **changes 字段是字符串数组**,每条一句话(≤40 字)
5. **score 字段 6 个键必须都存在**,每项 0-10,total 是 5 项之和
6. **若输入文本不含 AI 痕迹,rewritten 仍要返回改写后的版本**(轻度润色而非原样返回)

## 任务

1. 识别 AI 写作模式(下方分类)
2. 用自然学术替代方案重写问题片段
3. 保留原意与学术语气
4. 不引入第一人称、不引入"灵魂"

## 检测模式(全面改写)

### 八股句式(强制改写)

- "X 是 Y 的重要组成部分/关键环节" → 用具体机制替代
- "为 X 提供了新的视角/思路" → 删掉或换具体观点
- "对 X 进行了深入的探讨/分析" → 改为"考察"、"检视"、"辨析"
- "具有重要的理论意义和实践价值" → 删除
- "极大地/显著地促进了 X" → 改为具体机制或删除
- "对于 X 具有重要的启示意义" → 删除
- "在 X 方面取得了显著进展" → 改为具体进展描述
- "值得 X 借鉴/参考" → 改为具体借鉴点

### 话术堆叠

- 同段出现 "研究表明"、"学者认为"、"有学者指出"、"相关文献显示" → 删到 ≤1

### 强度词前缀

- "深入分析" → "考察"、"检视"、"辨析"
- "系统梳理" → "回顾"、"梳理"
- "全面探讨" → "剖析"
- "详细阐述" → "说明"
- "极大地/显著地" → 删除或换具体程度

### 完美段落结构

- 检测"主题句+引用+分析+小结"四段固定模板 → 打破至少一种
- 段落结尾多样化

### 模糊归因

- "行业专家认为"、"观察者指出" → 改为具体学者姓名

### 引用密度均匀

- LLM 综述每段引用密度几乎相等 → 重要观点段密集引,过渡段弱引或零引

### 学术专属规则(替代通用 humanizer 的"注入灵魂")

通用 humanizer 的"适当用我/允许跑题/承认复杂感受"等建议与学术综述规范冲突,以下 6 条为学术版替代:

- **不用第一人称** — 第三人称客观表述;可"笔者"但禁"我/我们";避免"你"
- **不中立无观点** — AI 综述常过度中立,要给学术判断:"本研究尚有不足"/"有待进一步验证"/"值得进一步探讨"
- **不跑题不题外话** — 每段聚焦一个论点;不要插入无关类比或逸闻
- **不抽象主张** — 用具体细节(年份/方法/样本规模/数据)而非"重要""显著""有效"
- **学者动作词轮换** — "考察"/"检视"/"梳理"/"辨析"/"剖析"/"管窥"/"审视"/"回顾"/"探究"代替单一"分析"
- **承认研究局限而非感受** — "本研究受样本量限制"/"该方法在 X 场景下可能不适用"代替"这令人担忧"

## 输入待润色文本

{{text}}

## 输出格式

**严格按下面 JSON 结构输出**,只输出一个 JSON object,其他什么都不要。

```json
{
  "rewritten": "改写后的完整文本(纯文本,不要任何 markdown 标记)",
  "changes": ["修改要点1", "修改要点2"],
  "score": {
    "direct": 8,
    "rhythm": 7,
    "trust": 9,
    "authenticity": 8,
    "conciseness": 7,
    "total": 39
  },
  "self_check": [
    "连续三个句子长度相同? 打断",
    "段落以简洁单行结尾? 变换",
    "揭示前有破折号? 删除",
    "解释隐喻? 相信读者",
    "使用'此外''然而'? 考虑删除",
    "三段式列举? 改为两项或四项",
    "每段'研究表明/学者认为'出现次数? ≤1",
    "句长方差 ≥8?"
  ]
}
```

**当 score_mode=false 时**:score 6 个键都填 0,total=0,但保留键名;self_check 可省略。
**示例**:

- rewritten: "农村末端物流配送的运行效率,直接关联农产品上行与工业品下行的双向流通成本,在县域经济结构转型中亦承担节点职能。末端环节若长期滞后,城乡要素流动的实际效能便受牵制,这一议题因而具备现实的检视必要。"
- changes: ["删除'此外'开场", "替换'X是Y的重要组成部分'八股句式", "用'检视'替换'深入探讨'"]
- score.direct: 8(score 0-10 表示改写后与原意保持度,8=高度保持)

```

```
