fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
pydantic>=2.6
openai>=1.40
# LangChain 写作编排(ChatPromptTemplate + RunnableLambda, 不做检索/向量化)
langchain-core>=0.3
python-dotenv>=1.0
sqlalchemy>=2.0
beautifulsoup4>=4.12
# 知网 HTTP 爬虫(嵌入:automation/cnki/crawler.py)依赖
requests>=2.31
urllib3>=2.0
lxml>=5.0
PyYAML>=6.0
pycryptodome>=3.19

# 核心期刊清单解析(backend/src/core_journals.py)
openpyxl>=3.1

# GB/T 7714-2015 引文渲染(citeproc-py 规则引擎 + csl-styles 官方 .csl 模板)
# 实际安装位置:backend/lib/(Anaconda site-packages 无写权限时,装到项目内)
# 如果部署环境 site-packages 有写权限,直接 pip install -r requirements.txt 即可
citeproc-py>=0.10,<0.11
