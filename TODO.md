# TODO / 未完成的工作

> 这次迭代触及的工作和未完成项。按优先级排序。

## 🔴 P0 — 影响主流程

### 1. 知网"高级检索"多行填表对 DOM 改版脆弱
- **现状**：`fill_query_into_search_box` 通过 JS 找"第一个 input 的祖先行" + 克隆 + 赋值。知网改版会破。
- **症状**：用户截图显示"主题主题主题..."重复填充，或 4 行全是同一段 query。
- **方向**：
  - 优先点页面原生的"+"按钮（已实现但有 fallback）
  - 用 `page.locator('input').nth(行号)` 替代 JS clone
  - 或：**让用户在浏览器画布里手动加行**（提供"复制 query 第一组到剪贴板"按钮）
- **建议**：增加 `/api/automation/clipboard` 端点 + 前端"复制到剪贴板"按钮作为 fallback。

### 2. 知网触发验证码时无优雅降级
- **现状**：`detect_verification(login)` 识别后只显示红字"请在画布里完成验证"，但浏览器依然执行后续 click/fill，**在验证页上操作 → 报错**。
- **症状**：截图里"检测到人机验证(login)，请在下方画布中完成验证"一直卡着。
- **方向**：
  - 检测到验证 → 暂停所有自动化操作 → 等待 user 在画布里完成 → 检测验证消失后**恢复**填表
  - 或：检测到验证 → 跳过该库 → 直接 switch 到下一个库（已经有 `advance_to_next_db`）

### 3. 远程浏览器会话丢失时 store 没清
- **现状**：`ustore.sessionId` 持久在 localStorage，但后端会清掉死会话。Store 不感知。
- **症状**：用户切回 tab → 看到"会话已恢复"标签 → 但其实后端已经 404。
- **方向**：
  - 在 `tryReconnectSession` 里如果 404 → `ustore.clearSession()` 已有，但前端没看到重连成功的明确反馈

## 🟡 P1 — 影响使用体验

### 4. 中文文献元数据抽取 HTML 解析对新版知网不健壮
- `html_extractor.py` 用硬编码的字段选择器
- 知网改版后作者/期刊/年份会抽不到

### 5. 万方/维普的填表与抽取策略还没细化
- 现在只支持 cnki 多行拆 + 通用单框
- 万方/维普被点开后只有一个普通搜索框，**URL 上加 kw 即可**（已经做了，但用户未验证）

### 6. 检索式生成"自动生成"还是依赖 LLM API
- 没设 `MINIMAX_API_KEY` 时走启发式兜底（已实现），但只覆盖有限主题
- 主题如"短视频对大学生心理健康的影响"有 5 个概念，"基于深度学习的医学影像诊断研究"也有 4 个
- 主题**不命中启发式词典**的会兜底成 1 个概念（用户已经反馈过）

### 7. 综述生成章节划分固定（"研究背景/现状/方法/评述"）
- 缺少对**实际文献**的引用编排
- 引文格式 GB/T 7714 校验缺失（脚注序号错乱 / 重复引用）
- 用户要求过但没全部实现

### 8. 检索历史表（`retrieval_tasks`）的"删除任务"按钮只加在英文检索页
- 统一检索页的"远程浏览器"会话**没有"删除任务"**的对应按钮（实际上不需要——浏览器会话直接关闭即可）

## 🟢 P2 — 改进类

### 9. 前端 `UnifiedRetrievalPage.vue` 已经 ~600 行
- 可以拆成 `<QueryForm>` / `<BrowserCanvas>` / `<CandidateList>` / `<ExtractBar>` 四个组件
- 维护性提升

### 10. 后端日志结构化
- 现在用 `log.info` 平铺
- 可以切到 `structlog` 或 `loguru`，方便查问题

### 11. WS 帧率 0.5s 一帧可能丢
- 知网复杂页面滚动时会丢帧
- 改成"按操作驱动"（只 click/scroll 后才推帧）

### 12. 英文库（OpenAlex）检索结果按引用量/相关性排序
- 现在按"search"接口默认排序
- 加排序参数让用户可控

### 13. 知识库/历史文献的复用
- 同一个主题之前跑过 → 直接恢复候选列表
- 现在每次都重新检索

## ✅ 已完成（本迭代）

- [x] 统一检索页（合并英文检索 + 中文导入 + 远程浏览器）
- [x] 中英双语布尔式生成（LLM + 启发式兜底）
- [x] 远程浏览器多 URL 轮询会话
- [x] 知网高级检索多行 AND 填表（JS 注入）
- [x] 自动循环翻页抽取 + 跨库 fallback
- [x] 前端状态持久化（Pinia + localStorage）
- [x] 检索任务删除（英文检索页 + 同步 store）
- [x] 综述 SSE 流式生成 + 人性化改写
- [x] GB/T 7714-2015 引用脚注（部分章节）
- [x] OpenAlex 异步检索任务
- [x] 文献池跨库浏览 / 选题 / 删除

## 📝 备注

### 关于知网"必须高级检索"
- 用户硬性要求走 AdvSearch 页面，不接受普通搜索
- 简化为"单输入框 + Enter"的方案被否决
- 当前方案：JS 注入 N-1 个新行 + 逐行赋值

### 关于"自动勾期刊"
- 早期实现过 `_cnki_journal_only()`，但 DOM 经常变（"期刊论文" vs "期刊" 文本变化）
- 用户后来要求"URL 上加 crossids 限定"被我说"做不到"——其实是可以的
- **下一步**：URL 上加 `crossids=YFZXHYLW&korder=SU` 限定"中国学术期刊网络出版总库"

### 关于 fill_query 的 bug
- Network Error 之前是 NameError 引起 500
- "卡在 loading"是 Playwright evaluate timeout 没设
- "只填 1 行"是 Playwright `nth(i)` 与行序错位
- 终极方案：**整段填表逻辑放在 JS 里一次完成**，避免 Python 端 Playwright 多次操作

## 待办（按时间紧迫程度）

1. **P0-1**：把"复制到剪贴板"加进统一检索页（用户可以手动粘贴到任何搜索框）
2. **P0-2**：验证页检测到 → 暂停 → 等待 → 恢复
3. **P1-4**：cnki HTML 抽取器写一个"模糊匹配"版本（按文本内容而不是 class/id）
4. **P1-7**：综述的引文序号校验（写一个后处理脚本，扫所有"[^N]"引用脚注，验证 N 唯一且连续）
5. **P2-11**：把 WS 帧率改成"按操作驱动"（mouse/keyboard 事件触发推帧，而不是 0.5s 轮询）

---

> 最后更新：2026-08-07
